env: {}

build:
  extVersion: 7.9.0
  ewcVersion: 7.5.0

config:
  title: Playground
  titleIcon: <i class="fa-solid fa-funnel-dollar"></i>

  theme:
    darkMode: auto
    baseColor: teal
    accentColor: red

  locales: [en-GB, en-US, uk-UA, ru-RU]

  pushNotifications:
    guestEnabled: false
    userEnabled: true

  firebase:
    web: ~
