dependencies:
  - "@softvisio/core"

services:
  api:
    components:
      locales: true
      threads: true
      api: true
      rpc: true
      webpack: true
      telegramNotificationsBot: true
      # nginx: true

defaultService: api

components:
  locales:
    locales: [en-GB, en-US, uk-UA, ru-RU]

  acl:
    types:
      main:
        roles:
          test:
            name: !l10nt [Test, { tags: backend }]
            description: !l10nt [Test scope, { tags: backend }]
            permissions:
              - acl:*

        notifications:
          test:
            name: !l10nt [ACL test, { tags: backend }]
            description: !l10nt [Test notifications, { tags: backend }]
            roles: ~
            channels:
              telegram: { editable: false, subscribed: true }

  notifications:
    types:
      test:
        name: !l10nt [Test, { tags: backend }]
        description: !l10nt [Test notifications, { tags: backend }]
        channels:
          internal: { editable: true }
          push: { editable: true }

config: ~
